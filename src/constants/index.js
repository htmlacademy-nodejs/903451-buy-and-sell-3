'use strict';

const {
  MAX_DIGIT,
  DEFAULT_COMMAND,
  DEFAULT_PORT,
  USER_ARGV_INDEX,
  UTF8_ENCODING_FORMAT,
  HOME_PATH,
  NOT_FOUND_MESSAGE_TEXT,
  ExitCode,
  HttpCode
} = require(`./common`);
const {
  DEFAULT_COUNT,
  MAX_COUNT,
  FILE_NAME,
  FILE_SENTENCES_PATH,
  FILE_TITLES_PATH,
  FILE_CATEGORIES_PATH,
  OfferType,
  SumRestrict,
  PictureRestrict,
  DescriptionRestrict,
} = require(`./mocksGeneration`);

module.exports = {
  MAX_DIGIT,
  DEFAULT_COMMAND,
  DEFAULT_PORT,
  USER_ARGV_INDEX,
  UTF8_ENCODING_FORMAT,
  HOME_PATH,
  NOT_FOUND_MESSAGE_TEXT,
  ExitCode,
  HttpCode,
  DEFAULT_COUNT,
  MAX_COUNT,
  FILE_NAME,
  FILE_SENTENCES_PATH,
  FILE_TITLES_PATH,
  FILE_CATEGORIES_PATH,
  OfferType,
  SumRestrict,
  PictureRestrict,
  DescriptionRestrict,
};
