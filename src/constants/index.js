'use strict';

const {
  MAX_DIGIT,
  DEFAULT_COMMAND,
  USER_ARGV_INDEX,
  UTF8_ENCODING_FORMAT,
  ExitCode,
} = require(`./common`);
const {
  DEFAULT_COUNT,
  MAX_COUNT,
  FILE_NAME,
  FILE_SENTENCES_PATH,
  FILE_TITLES_PATH,
  FILE_CATEGORIES_PATH,
  OfferType,
  SumRestrict,
  PictureRestrict,
  DescriptionRestrict,
} = require(`./mocksGeneration`);

module.exports = {
  MAX_DIGIT,
  DEFAULT_COMMAND,
  USER_ARGV_INDEX,
  UTF8_ENCODING_FORMAT,
  ExitCode,
  DEFAULT_COUNT,
  MAX_COUNT,
  FILE_NAME,
  FILE_SENTENCES_PATH,
  FILE_TITLES_PATH,
  FILE_CATEGORIES_PATH,
  OfferType,
  SumRestrict,
  PictureRestrict,
  DescriptionRestrict,
};
